const shoppingList = []
console.log(Purlist)

shoppingList[0] = 'bread'
shoppingList[1] = 'eggs'
shoppingList[2] = 45
shoppingList[5] = 'fish'
//You can determine an array element later in the list, and by doing so, the spaces before it that have not yet been instantiated will be empty.

console.log(shoppingList) //prints the entire array  ['bread', 'eggs', 45, 'fish']
console.log(shoppingList[2]) //prints the array item referenced by the index    [45]
